

<div class="max-w-7xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold text-gray-800 mb-6">History</h1>
  
  <% if (attempts.length === 0) { %>
    <div class="bg-white rounded-lg shadow-md p-6 text-center">
      <p class="text-gray-600">You have not taken any tests yet.</p>
      <a href="/user/tests" class="inline-block mt-4 bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-md transition duration-200">
        View test list
      </a>
    </div>
  <% } else { %>
    <div class="bg-white rounded-lg shadow-md overflow-hidden">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Test
            </th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Score
            </th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Percentage
            </th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Time
            </th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Action
            </th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% attempts.forEach(attempt => { %>
            <tr>
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="text-sm font-medium text-gray-900">
                  <%= attempt.test ? attempt.test.title : 'Test has been deleted' %>
                </div>
              </td>
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="text-sm text-gray-900">
                  <%= attempt.score %> / <%= attempt.totalPossibleScore %>
                </div>
              </td>
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="text-sm text-gray-900">
                  <% 
                    // Kiểm tra percentageScore tồn tại
                    const percentageScore = attempt.percentageScore !== undefined ? 
                      attempt.percentageScore : 
                      (attempt.totalPossibleScore > 0 ? 
                        (attempt.score / attempt.totalPossibleScore) * 100 : 0);
                  %>
                  <%= percentageScore.toFixed(1) %>%
                </div>
              </td>
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="text-sm text-gray-900">
                  <%= new Date(attempt.completedAt).toLocaleString('vi-VN') %>
                </div>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                <a href="/user/results/<%= attempt.test ? attempt.test._id : '#' %>" class="text-blue-600 hover:text-blue-900">
                  View details
                </a>
              </td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    </div>
  <% } %>
</div>
